[package]
name = "ai-usage-mit"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "ai-usage"
path = "src/main.rs"

[dependencies]
serde_json = "1"
chrono = { version = "0.4", default-features = false, features = ["std", "clock"] }
